<?php
    // ARITHMETIC OPERATORS
    // + - * / ** %

    $x = 10;
    $y = 5;
    $z = null;

    // $z = $x + $y;
    // $z = $x - $y;
    // $z = $x * $y;
    // $z = $x / $y;
    // $z = $x ** $y;
    $z = $x % $y;
    echo $z;



    // INCREMENT / DECREMENT OPERATORS
    
    $counter = 0;

    $counter = $counter + 1;

    // $counter++ to increment by 1
    $counter++;
    // or
    // $counter += 2; to increment by 2
    $counter += 2;

    // $counter-- to decrement by 1;
    $counter--;
    echo $counter;
?>


